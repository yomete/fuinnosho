import NowPage from "@/components/now/now-page";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Now · Fuinnosho",
};

export default function Now() {
  return <NowPage />;
}
